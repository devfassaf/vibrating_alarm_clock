<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark-only app; the dark window background avoids a white flash at launch. -->
    <style name="Theme.VibeAlarm" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
